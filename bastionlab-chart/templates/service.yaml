apiVersion: v1
kind: Service
metadata:
  name: {{ include "bastionlab.fullname" . }}
  labels:
    {{- include "bastionlab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bastionlab.selectorLabels" . | nindent 4 }}
