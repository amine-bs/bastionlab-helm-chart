apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bastionlab.fullname" . }}-test-connection"
  labels:
    {{- include "bastionlab.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bastionlab.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
